var _parcelHelpers = require("@parcel/transformer-js/lib/esmodule-helpers.js");
_parcelHelpers.defineInteropFlag(exports);
_parcelHelpers.export(exports, "getPartData", function () {
  return getPartData;
});
var global = arguments[3];
/*eslint-disable no-console*/
// const fetch = require('fetch');
const BASE_URL = 'https://jsonplaceholder.typicode.com';
const partData = [];
const getDataFromServer = data => {
  return global.fetch(`${BASE_URL}/${data}`).then(response => !response.ok ? new Error(`${response.status} - ${response.statusText}`) : response.json());
};
const getPartData = (data, start, end) => {
  getDataFromServer(data).then(dataFromServer => {
    dataFromServer.slice(start, end).map(post => {
      partData.push(post);
    });
  });
  return partData;
};
