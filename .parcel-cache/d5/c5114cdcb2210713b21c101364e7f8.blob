var _parcelHelpers = require("@parcel/transformer-js/lib/esmodule-helpers.js");
_parcelHelpers.defineInteropFlag(exports);
_parcelHelpers.export(exports, "getPosts", function () {
  return getPosts;
});
var _apiApi = require('../api/api');
async function getPosts() {
  const startIndex = 0;
  const lastIndex = 20;
  const getPosts = await _apiApi.getDataFromServer('posts', startIndex, lastIndex);
  const container = document.querySelector('#posts');
  let posts = '';
  getPosts.forEach(post => {
    const postHtml = `
      <div class="posts__post post">
        <h3 class="post__title">${post.title}</h3>
        <p class="post__text">${post.body}</p>
      </div>`;
    posts += postHtml;
  });
  container.innerHTML = posts;
}
// function renderPosts() {
// if ()
// }
window.addEventListener('scroll', () => {
  let windowScrollValue = window.innerHeight + window.scrollY;
  // eslint-disable-next-line no-console, max-len
  console.log(scrollValue === document.body.offsetHeight);
});
getPosts();
